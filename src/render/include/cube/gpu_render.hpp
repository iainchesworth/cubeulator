#pragma once

#include <cube/error.hpp>

namespace cube::gpu_render {

// Placeholder entry point for bgfx setup and the technical-drawing render
// pipeline. Real logic lands with the render pipeline spec.
[[nodiscard]] Result<void> not_yet_implemented();

}  // namespace cube::gpu_render
