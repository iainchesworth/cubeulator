#pragma once

// NOTE: this header is generated from version.hpp.in by cmake/GenerateVersion.cmake.
// Do not edit the generated copy; edit the template instead.

#include <string>
#include <string_view>

namespace cube {

// Semantic version (https://semver.org).
inline constexpr int version_major = @CUBEULATOR_VERSION_MAJOR@;
inline constexpr int version_minor = @CUBEULATOR_VERSION_MINOR@;
inline constexpr int version_patch = @CUBEULATOR_VERSION_PATCH@;
inline constexpr std::string_view version_string = "@CUBEULATOR_VERSION@";

// Git provenance, stamped at build time.
inline constexpr std::string_view git_commit = "@CUBEULATOR_GIT_COMMIT@";
inline constexpr std::string_view git_commit_full = "@CUBEULATOR_GIT_COMMIT_FULL@";
inline constexpr std::string_view git_describe = "@CUBEULATOR_GIT_DESCRIBE@";
inline constexpr std::string_view git_branch = "@CUBEULATOR_GIT_BRANCH@";
inline constexpr bool git_dirty = @CUBEULATOR_GIT_DIRTY@;

// A multi-line, human-readable version + build-provenance block, suitable for
// `--version` output or an About dialog.
[[nodiscard]] std::string version_details();

}  // namespace cube
